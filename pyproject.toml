[project]
name = "geointel"
version = "1.0.0"
description = "OSINT intelligence from your terminal"
requires-python = ">=3.10"

[project.scripts]
geointel = "app.__main__:main"

[tool.setuptools.packages.find]
include = ["app*", "api*"]
exclude = ["frontend*", "data*", "screenshots*"]
